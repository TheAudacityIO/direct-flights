node_modules
.output
.nitro
.tanstack
.vercel
dist
data/raw
*.log
.DS_Store
.env
.env.*
.vinxi
screenshots
artifacts
# Grok-platform workspace state stays local, but the shipped build flags and
# PWA assets are part of the repo (CI and fresh clones need them).
.grok/*
!.grok/app-env.json
AGENTS.project.md
startup.sh
.node_modules.lock
.claude/
